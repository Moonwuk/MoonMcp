"""Curated security knowledge bases MoonMCP can serve to an agent.

Currently: a structured injection knowledge base (patterns, root causes and
detection signatures) exposed via tools and the ``injections://`` resource.
"""
