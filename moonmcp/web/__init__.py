"""Web-application recon checks (CORS, GraphQL, HTTP methods, WAF, JWT, takeover).

All are cheap, high-signal, standard-library heuristics — the "quick wins" of a
bug-bounty first pass — and every one that sends traffic is scope-gated by the
server layer.
"""
